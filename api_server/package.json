{
  "name": "ahribori_api_server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir build",
    "start": "cross-env NODE_ENV=production node ./build/server.js",
    "boot": "cross-env NODE_ENV=production node ./build/server.js",
    "dev": "cross-env NODE_ENV=development nodemon --exec babel-node ./src/server.js --watch src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.21.0",
    "babel-plugin-transform-object-rest-spread": "^6.22.0",
    "babel-preset-latest": "^6.16.0"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "mongoose": "^4.7.6",
    "morgan": "^1.7.0",
    "multiparty": "^4.1.3",
    "request": "^2.79.0"
  }
}
