{
  "name": "auth_server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir build --presets=latest",
    "start": "cross-env NODE_ENV=production node ./build/server.js",
    "boot": "cross-env NODE_ENV=production node ./build/server.js",
    "dev": "cross-env NODE_ENV=development nodemon --exec babel-node --presets=es2015 ./src/server.js --watch src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "jsonwebtoken": "^7.2.1",
    "mongoose": "^4.7.6",
    "morgan": "^1.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.21.0",
    "babel-preset-latest": "^6.16.0"
  }
}
